

const Craw = () => {
    // const [data, setData] = useState([]);
    // const [loading, setLoading] = useState(false);
    // const [url, setUrl] = useState('');
    // const getData = async () => {
    //     setLoading(true);
    //     const res = await api.get(`http://localhost:5000/get_data`);
    //     setData(res.data);
    //     setLoading(false);
    // }

    return (
        <div>
            {/* <input value={url} onChange={(e)=>setUrl(e.target.value)}/>
            <button onClick={getData}>go</button>
            {loading? <div>로딩 중..</div> :data.map((item, index) => (
                <a key={index} href={item[0]} target="_blank" rel="noopener noreferrer">
                    <img src={item[1]} alt="" width={200} />
                </a>
            ))} */}
        </div>
    );
};

export default Craw;